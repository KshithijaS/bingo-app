{{form.username.label}}
                {{form.username}}

                {{form.email.label}}
                {{form.email}}

                {{form.password1.label}}
                {{form.password1}}

                {{form.password2.label}}
                {{form.password2}}
                
                <input type="submit" class="btn btn-primary" value="submit">


                <form method="POST">
                    {% csrf_token %}
                    <div class="form-group">
                    <input type="email" class="form-control credentials" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="E-mail">
                    <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
                    </div>
                    <div class="form-group">
                        <input type="Username" class="form-control credentials" id="exampleInputUsername" placeholder="Username">
                    </div>
                    <div class="form-group">
                    <input type="password" class="form-control credentials" id="examplePassword" placeholder="Create Password">
                    </div>
                    <div class="form-group">
                        <input type="password" class="form-control credentials" id="examplePassword" placeholder="Confirm Password">
                    </div>
                    <div class="button">
                        <a href="{% url 'signup' %}" alt="Play-now" ><button type="button" class="btn btn-md rounded-pill" id="custom-btn">Sign-Up</button></a>
                    </div>
                    {{form.errors}}
                    <p id="signup-link"><center>Already have an account? <a href="{% url 'signup' %}">Login</a></center></p>
                    
                </form>